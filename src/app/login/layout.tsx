export const metadata = {
  title: "Sign In",
  description: "Sign in to PhilosophieBook to join philosophical debates, comment, vote, and propose topics.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
