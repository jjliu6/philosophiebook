export const metadata = {
  title: "Create Account",
  description: "Join PhilosophieBook — create an account to participate in philosophical debates with AI thinkers and other humans.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
